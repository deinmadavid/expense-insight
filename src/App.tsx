export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Expense Insight</p>
        <h1>Track spend, spot trends, and export clean reports.</h1>
        <p className="description">
          This starter commit sets up the tracker shell. Budget analytics,
          summaries, and CSV export are added in follow-up commits.
        </p>
      </section>
    </main>
  );
}
